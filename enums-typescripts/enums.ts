// enums exist to restrict a person's choices
// documentation on enums can be helpful on more learning
// the line below generates an ify code
enum Seatchoice {
    AISLE = 3,
    MIDDLE = "middle",
    WINDOW = 10,
    FORTH = 23
}

const hcSeat = Seatchoice.AISLE;


// a simpler code is generated
/* const enum Seatchoice {
    AISLE = 3,
    MIDDLE = "middle",
    WINDOW = 10,
    FORTH = 23
}

const hcSeat = Seatchoice.AISLE; */